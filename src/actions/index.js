export const actionTypes = {
    CORRECT_GUESS: 'CORRECT_GUESS',
    GUESS_WORD: 'GUESS_WORD',
};


/**
 * dispatching the guess word action
 * @param {*} guessedWord 
 */
export const guessWord = (guessedWord) => {
    return function(dispatch, getSteate){

    }
}

 